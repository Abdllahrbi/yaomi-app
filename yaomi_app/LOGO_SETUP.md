# 🎨 إضافة اللوجو للتطبيق

## الخطوات

### 1. حفظ ملف اللوجو

احفظ صورة اللوجو في المسار التالي:

```
yaomi_app/assets/logo.png
```

**ملاحظة:** 
- اسم الملف يجب أن يكون `logo.png` بالضبط
- المجلد `assets` موجود بالفعل
- يفضل أن تكون الصورة PNG بخلفية شفافة

---

### 2. التحقق من الإعداد

تم تحديث `pubspec.yaml` بالفعل لتضمين:

```yaml
flutter:
  uses-material-design: true
  
  assets:
    - assets/logo.png
```

---

### 3. إعادة تشغيل التطبيق

بعد إضافة الصورة، قم بـ:

```bash
# في terminal
flutter pub get
flutter run
```

أو في VS Code:
- اضغط `Shift + F5` (Stop)
- ثم اضغط `F5` (Run)

---

## 🎯 أين يظهر اللوجو؟

تم إضافة اللوجو في:

1. ✅ **Login Screen** - في الأعلى (120px)
2. ✅ **Register Screen** - في الأعلى (100px)
3. ✅ **Dashboard Welcome Card** - في اليمين (60px)
4. ✅ **About Dialog** - في المنتصف (80px)

---

## 🖼️ مواصفات اللوجو المثالية

### للاستخدام الحالي
- **الصيغة:** PNG
- **الخلفية:** شفافة
- **الأبعاد:** 512x512 أو أكبر
- **الحجم:** أقل من 500KB

### للنشر في App Stores
ستحتاج لاحقاً:

#### Android (Google Play)
- Icon: 512x512 PNG
- Feature Graphic: 1024x500 PNG
- Screenshots: 1080x1920 - 1080x2340

#### iOS (App Store)
- Icon: 1024x1024 PNG (بدون alpha channel)
- Screenshots: حسب الأجهزة

---

## 🔧 إذا لم يظهر اللوجو

### الحل 1: Hot Restart
```bash
# في terminal
r  # للـ hot reload
R  # للـ hot restart (أفضل للـ assets)
```

### الحل 2: Clean & Rebuild
```bash
flutter clean
flutter pub get
flutter run
```

### الحل 3: تحقق من المسار
تأكد من:
- المجلد اسمه `assets` (بالحرف الصغير)
- الملف اسمه `logo.png` (بالحرف الصغير)
- الملف موجود في: `yaomi_app/assets/logo.png`

---

## 📱 معاينة التصميم

### Login Screen
```
┌─────────────────────┐
│                     │
│                     │
│      [LOGO]         │
│                     │
│  ┌───────────────┐  │
│  │ Email         │  │
│  └───────────────┘  │
│  ┌───────────────┐  │
│  │ Password      │  │
│  └───────────────┘  │
│                     │
│  [  Login Button ]  │
│                     │
│   Register Link     │
└─────────────────────┘
```

### Dashboard Welcome Card
```
┌─────────────────────────────┐
│ 👋 Welcome to    [LOGO]     │
│ Your smart AI kitchen       │
│ assistant                   │
└─────────────────────────────┘
```

---

## ✅ Checklist

- [ ] نسخ ملف اللوجو إلى `yaomi_app/assets/logo.png`
- [ ] تشغيل `flutter pub get`
- [ ] تشغيل التطبيق `flutter run`
- [ ] التحقق من ظهور اللوجو في Login Screen
- [ ] التحقق من ظهور اللوجو في Dashboard

---

## 🎨 نصائح التصميم

### استخدام الصور المتعددة من اللوجو

اللوجو الموجود يحتوي على 3 نسخ:
1. **أفقي مع النص** - استخدمناه في Login/Register
2. **أيقونة فقط** - يمكن استخدامها كـ App Icon
3. **أفقي Outline** - يمكن استخدامها في الـ Splash Screen

### لاستخراج الأيقونة فقط
إذا أردت استخدام الأيقونة فقط (الجزء الأخضر):
1. افتح الصورة في أي محرر صور
2. قص الجزء السفلي (الأيقونة فقط)
3. احفظها كـ `logo_icon.png`
4. أضفها في `assets` واستخدمها حيث تريد

---

تم! 🎉 اللوجو جاهز للاستخدام في التطبيق.
